{
  "name": "ai-braking-deployment",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "postinstall": "npm ci --prefix website --include=dev && npm run build --prefix website",
    "build": "npm run build --prefix website",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "wrangler": "4.129.0" }
}
